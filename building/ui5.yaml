# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "2.5"
metadata:
  name: building
type: application
server:
  customMiddleware:
    - name: ui5-middleware-simpleproxy
      afterMiddleware: compression
      mountPath: /sap/opu/odata
      configuration:
        baseUri: "https://ciiwsxs409.iws.all-for-one.cloud:44309/sap/opu/odata/"
        strictSSL: false        
sap//$metadata